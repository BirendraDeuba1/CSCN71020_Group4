#include "rectangle.h"

int calculate_area(int length, int breadth) { //added the functionality to get the area of triangle.
    return length * breadth ;
}

int calculate_perimeter(int length, int breadth) { //added the functionality to get the perimeter of triangle
    return (length + length + breadth);
}